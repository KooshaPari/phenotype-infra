# =============================================================================
# Trunk Check — Unified linting/formatting in GitHub Actions
# =============================================================================
# Handles: ruff, mypy, clippy, golangci-lint, prettier, eslint, shellcheck, etc.
# Free for open source; cached for fast runs
# =============================================================================

name: Trunk Check

permissions:
  contents: read
  checks: write

on:
  pull_request:
  push:
    branches: [main, develop]
  schedule:
    - cron: "0 3 * * 1" # Weekly Monday 3am UTC

concurrency:
  group: trunk-${{ github.ref }}
  cancel-in-progress: true

jobs:
  trunk-check:
    name: Lint & Format
    runs-on: ubuntu-latest
    timeout-minutes: 10

    steps:
      - name: Checkout
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2

      - name: Install Trunk tools
        # The check action reports missing managed binaries on a cold cache.
        # Install them before the check so every enabled linter is executable.
        uses: >-
          trunk-io/trunk-action/install@c146ebe205e2e4534218dc9b7041056c43b3b7a0

      - name: Trunk Check
        uses: >-
          trunk-io/trunk-action@c146ebe205e2e4534218dc9b7041056c43b3b7a0
        with:
          # Install the enabled plugin binaries on a cold runner; relying on
          # the action cache makes first-run checks fail as "binary not found".
          setup-deps: true

      - name: Trunk Upgrade (on schedule only)
        if: github.event_name == 'schedule'
        uses: >-
          trunk-io/trunk-action@c146ebe205e2e4534218dc9b7041056c43b3b7a0
        with:
          trunk-args: --upgrade
