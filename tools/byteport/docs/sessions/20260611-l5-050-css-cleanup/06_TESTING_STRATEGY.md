# Testing Strategy

- Run the frontend type and Svelte validation via `npm run check`.
- Run the frontend production build via `npm run build`.
- If either fails for unrelated reasons, document the failure and still commit the cleanup plus worklog.
