# A26 — Orphaned Configs Sweep (configs/)

**Date:** 2026-06-25  
**Branch:** `dag-A26-2026-06-25`  
**Scope:** All files under `configs/`

---

## Inventory

| # | Config File | Service |
|---|-------------|---------|
| 1 | `configs/cloudflared/config.yml.example` | Cloudflare Tunnel ingress routing |
| 2 | `configs/forgejo/app.ini.example` | Forgejo git server configuration |
| 3 | `configs/vaultwarden/config.env.example` | Vaultwarden secrets store |
| 4 | `configs/woodpecker/server.env.example` | Woodpecker CI server env |

---

## Cross-Reference Analysis

### 1. `configs/cloudflared/config.yml.example`

| Reference Location | Type | Evidence |
|---|---|---|
| `docs/runbooks/day2-cloudflare-tunnel.md:31` | Runbook (path reference) | `sudo cp configs/cloudflared/config.yml.example /etc/cloudflared/config.yml` |
| `iac/ansible/playbooks/templates/cloudflared-config.yml.j2` | Ansible template (counterpart) | The Ansible playbook `install-cloudflared.yml` templates this J2 file to the same destination |

**Verdict: IN-USE** — Directly referenced in the Day 2 tunnel runbook and backed by an Ansible template.

### 2. `configs/forgejo/app.ini.example`

| Reference Location | Type | Evidence |
|---|---|---|
| `iac/ansible/playbooks/templates/forgejo-app.ini.j2` | Ansible template (counterpart) | The Ansible playbook `install-forgejo.yml` templates this J2 file to `/etc/forgejo/app.ini` |
| `iac/ansible/playbooks/install-forgejo.yml:104-105` | Playbook | `src: forgejo-app.ini.j2`, `dest: "{{ forgejo_etc }}/app.ini"` |

**Verdict: IN-USE** — Backed by a live Ansible template counterpart.

### 3. `configs/vaultwarden/config.env.example`

| Reference Location | Type | Evidence |
|---|---|---|
| `iac/ansible/playbooks/install-vaultwarden.yml:97-110` | Playbook (inline env vars) | The Ansible playbook writes `/etc/vaultwarden/.env` with the same variables documented in the example (`DATA_FOLDER`, `DATABASE_URL`, etc.) |

**Verdict: IN-USE** — Documents the env vars that the Ansible playbook writes to the actual `.env` file.

### 4. `configs/woodpecker/server.env.example`

| Reference Location | Type | Evidence |
|---|---|---|
| `iac/ansible/playbooks/install-woodpecker.yml:83-99` | Playbook (inline env vars) | The Ansible playbook writes `/etc/woodpecker/server.env` with the same variables (`WOODPECKER_GITEA_URL`, `WOODPECKER_DATABASE_DRIVER`, etc.) |

**Verdict: IN-USE** — Documents the env vars that the Ansible playbook writes to the production `server.env`.

---

## Summary

| Config File | Status | Notes |
|---|---|---|
| `cloudflared/config.yml.example` | IN-USE | Runbook reference + Ansible template counterpart |
| `forgejo/app.ini.example` | IN-USE | Ansible template counterpart (`forgejo-app.ini.j2`) |
| `vaultwarden/config.env.example` | IN-USE | Documents inline vars in Ansible playbook |
| `woodpecker/server.env.example` | IN-USE | Documents inline vars in Ansible playbook |

**Final Verdict: No orphaned configs found.**

All four `.example` files serve as developer documentation/reference for the real configs managed by Ansible. Each corresponds to an active service (cloudflared, forgejo, vaultwarden, woodpecker) deployed via `iac/ansible/playbooks/`.

### Recommended Actions

| Action | Priority | Detail |
|--------|----------|--------|
| Keep all four files | None (retain) | They serve as offline-reference for manual setup and documentation of config shape |
| (Optional) Add cross-reference comments | Low | Each `.example` could include a comment at the top directing readers to the Ansible counterpart |

---

*Generated by DAG unit A26.*
