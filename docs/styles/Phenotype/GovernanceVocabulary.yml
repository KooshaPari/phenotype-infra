extends: substitution
message: "Use '%s' instead of '%s' for Phenotype governance terminology."
level: warning
ignorecase: true
swap:
  stage gate: stage-gate
  stage gates: stage-gates
  policy gate: policy-gate
  policy gates: policy-gates
  pull request: PR
  canary first: canary-first
  forward only: forward-only
  code owners: CODEOWNERS
